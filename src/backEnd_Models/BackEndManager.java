/*
 * This program was designed for a arduino Uno, or any similar device that can communicate over 
 * serial USB.
 * ---------------------------------------------------------
 * This program is designed to have the arduino itself run custom code, so do not expect
 * that you can run this on any random arduino. The code required to get the arduino to work
 * can be found as a .ino file recognizable by the arduino IDE, within the zip package of this
 * program.
 */
package backEnd_Models;

/**
 *  This is meant to run on a thread created from ModelsAndViewsControllerFX,
 * and manages both the SerialComms class, and the PixelControl class. Acts as the glue between
 * serial communication, pattern management, and the frontend GUI.
 * @author kell-gigabyte
 */
public class BackEndManager {
    
}
